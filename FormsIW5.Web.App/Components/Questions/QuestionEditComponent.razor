@using FormsIW5.Common.Enums
<h3>QuestionEditComponent</h3>

<button type="button" class="btn-danger" @onclick="DeleteQuestionAsync">
    Delete
</button>

<EditForm Model="@Model">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label class="w-100">
                    Question Name:
                    <InputText @bind-Value="@Model.QuestionText" class="form-control" @onfocusout="UpdateAsync" />
                </label>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label class="w-100">
                    Question Description:
                    <InputTextArea @bind-Value="@Model.Description" class="form-control" @onfocusout="UpdateAsync" />
                </label>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label class="w-100 font-weight-bold">
                    <InputSelect @bind-Value="Model.QuestionType" class="form-control" @onfocusout="UpdateAsync">
                        @foreach (var foodType in Enum.GetValues<QuestionType>())
                        {
                            <option value="@foodType">@foodType.GetReadableName()</option>
                        }
                    </InputSelect>
                </label>
            </div>
        </div>
    </div>

    @if (Model.QuestionType == QuestionType.Multiselection)
    {
        <div class="horizontal-selection-container">
            @foreach (var option in Model.QuestionOptions)
            {
                <QuestionOptionComponent OnChanged="UpdateAsync" OnDelete="DeleteQuestionOptionAsync" QuestionOption="@option" />
            }
        </div>
        <button @onclick="@AddQuestionOptionAsync">Add Option</button>
    }
    else if (Model.QuestionType == QuestionType.RangeType)
    {
        <div class="col-md-6">
            <div class="form-group">
                <label class="w-100 font-weight-bold mb-0" for="durationInput">
                    Range
                </label>
                <div class="input-group">
                    <div class="p-2">Min:</div>
                    <InputNumber min="0" @bind-Value="Model.FromValue" class="form-control" id="durationInput" @onfocusout="UpdateAsync" />
                    <div class="p-2">Max:</div>
                    <InputNumber min="0" @bind-Value="Model.ToValue" class="form-control" @onfocusout="UpdateAsync" />
                </div>
            </div>
        </div>
    }

</EditForm>